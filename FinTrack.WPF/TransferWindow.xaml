<Window x:Class="FinTrack.WPF.TransferWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Para Transferi" Height="360" Width="400"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Transfer İşlemi" Grid.ColumnSpan="2" FontSize="18" FontWeight="Bold" Margin="0,0,0,20"/>

        <!-- Date -->
        <TextBlock Text="Tarih:" Grid.Row="1" VerticalAlignment="Center"/>
        <DatePicker x:Name="DatePicker" Grid.Row="1" Grid.Column="1" Margin="0,0,0,10" xmlns:System="clr-namespace:System;assembly=mscorlib" SelectedDate="{x:Static System:DateTime.Now}"/>

        <!-- Source Account -->
        <TextBlock Text="Gönderen (Kaynak):" Grid.Row="2" VerticalAlignment="Center"/>
        <ComboBox x:Name="SourceCombo" Grid.Row="2" Grid.Column="1" Margin="0,0,0,10" SelectionChanged="SourceTarget_SelectionChanged"/>

        <!-- Target Account -->
        <TextBlock Text="Alıcı (Hedef):" Grid.Row="3" VerticalAlignment="Center"/>
        <ComboBox x:Name="TargetCombo" Grid.Row="3" Grid.Column="1" Margin="0,0,0,10" SelectionChanged="SourceTarget_SelectionChanged"/>

        <!-- Amount -->
        <TextBlock Text="Tutar:" Grid.Row="4" VerticalAlignment="Center"/>
        <TextBox x:Name="AmountTextBox" Grid.Row="4" Grid.Column="1" Margin="0,0,0,10" Padding="2" FontSize="14" TextChanged="AmountTextBox_TextChanged"/>

        <!-- Description -->
        <TextBlock Text="Açıklama:" Grid.Row="5" VerticalAlignment="Top" Margin="0,5,0,0"/>
        <TextBox x:Name="DescriptionTextBox" Grid.Row="5" Grid.Column="1" Margin="0,0,0,10" TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto"/>

        <!-- Buttons -->
        <StackPanel Grid.Row="6" Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="CancelButton" Content="İptal" Width="80" Margin="0,0,10,0" Padding="5" Click="CancelButton_Click"/>
            <Button x:Name="SaveButton" Content="Transfer Et" Width="100" Padding="5" Click="SaveButton_Click" IsDefault="True" Background="#2980B9" Foreground="White" BorderThickness="0">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="4"/>
                    </Style>
                </Button.Resources>
            </Button>
        </StackPanel>
    </Grid>
</Window>
