<Window x:Class="FinTrack.WPF.Views.CategoryEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Kategori Düzenle" Height="300" Width="400" 
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="#F4F7F6">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="🏷️ Kategori Düzenle" FontSize="18" FontWeight="Bold" Foreground="#2C3E50" Margin="0,0,0,20"/>

        <!-- Kategori Adı -->
        <StackPanel Grid.Row="1" Margin="0,0,0,15">
            <TextBlock Text="Kategori Adı" FontSize="12" Foreground="#7F8C8D" Margin="0,0,0,5"/>
            <TextBox x:Name="CategoryNameTextBox" Padding="8" FontSize="14" BorderBrush="#D5DBDB" Background="White"/>
        </StackPanel>

        <!-- Üst Kategori -->
        <StackPanel Grid.Row="2" Margin="0,0,0,15">
            <TextBlock Text="Üst Kategori" FontSize="12" Foreground="#7F8C8D" Margin="0,0,0,5"/>
            <ComboBox x:Name="ParentCategoryComboBox" Padding="8" FontSize="14" DisplayMemberPath="FullDisplayName"/>
        </StackPanel>

        <!-- Butonlar -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="CancelButton" Content="Vazgeç" Padding="20,8" Margin="0,0,10,0" 
                    Background="#95A5A6" Foreground="White" BorderThickness="0" Cursor="Hand" Click="CancelButton_Click">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="4"/>
                    </Style>
                </Button.Resources>
            </Button>
            <Button x:Name="SaveButton" Content="💾 Kaydet" Padding="25,8" 
                    Background="#27AE60" Foreground="White" FontWeight="Bold" BorderThickness="0" Cursor="Hand" Click="SaveButton_Click">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="4"/>
                    </Style>
                </Button.Resources>
            </Button>
        </StackPanel>
    </Grid>
</Window>
