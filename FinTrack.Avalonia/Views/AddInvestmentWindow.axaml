<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="using:FinTrack.Avalonia.ViewModels"
        xmlns:models="using:FinTrack.Core.Models"
        x:Class="FinTrack.Avalonia.Views.AddInvestmentWindow"
        x:DataType="vm:AddInvestmentViewModel"
        Title="Yatırım Satın Al" Height="670" Width="470"
        WindowStartupLocation="CenterOwner" CanResize="False" Background="#F4F6F9">
    
    <Grid Margin="20" RowDefinitions="Auto,*,Auto,Auto">

        <TextBlock Grid.Row="0" Text="Yeni Yatırım Alışı" FontSize="20" FontWeight="Bold" Foreground="#2C3E50" Margin="0,0,0,20"/>

        <!-- Error Message -->
        <TextBlock Grid.Row="0" Text="{Binding ErrorMessage}" Foreground="Red" IsVisible="{Binding HasError}" VerticalAlignment="Bottom" Margin="0,0,0,5"/>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="0,0,0,10">
            <StackPanel Spacing="5" Margin="0,0,10,0">
                <TextBlock Text="Varlık Sembolü (Örn. FENER, XAU, USD)" FontSize="12" Foreground="#34495E" FontWeight="SemiBold"/>
                <AutoCompleteBox Padding="10" FontSize="14" Margin="0,0,0,10"
                                 Text="{Binding Symbol}"
                                 ItemsSource="{Binding AvailableSymbols}"
                                 SelectedItem="{Binding SelectedSymbolItem}"
                                 ValueMemberBinding="{Binding Symbol, DataType=models:SymbolItem}"
                                 FilterMode="Custom"
                                 ItemFilter="{Binding SymbolFilter}"
                                 PlaceholderText="Sembol arayın... (Örn: THYAO)">
                    <AutoCompleteBox.ItemTemplate>
                        <DataTemplate x:DataType="models:SymbolItem">
                            <TextBlock Text="{Binding DisplayText}"/>
                        </DataTemplate>
                    </AutoCompleteBox.ItemTemplate>
                </AutoCompleteBox>

                <TextBlock Text="Varlık Adı (örn. Gram Altın)" FontSize="12" Foreground="#34495E" FontWeight="SemiBold"/>
                <TextBox Padding="10" FontSize="14" Margin="0,0,0,10" Text="{Binding Name}"/>

                <TextBlock Text="Kategori" FontSize="12" Foreground="#34495E" FontWeight="SemiBold"/>
                <ComboBox Padding="10" FontSize="14" Margin="0,0,0,10" HorizontalAlignment="Stretch" ItemsSource="{Binding Categories}" SelectedItem="{Binding SelectedCategory}"/>

                <TextBlock Text="İşlem Tarihi" FontSize="12" Foreground="#34495E" FontWeight="SemiBold"/>
                <CalendarDatePicker Padding="10" FontSize="14" Margin="0,0,0,10" HorizontalAlignment="Stretch" SelectedDate="{Binding SelectedDate}"/>

                <Grid Margin="0,0,0,10" ColumnDefinitions="*,10,*">
                    <StackPanel Grid.Column="0" Spacing="5">
                        <TextBlock Text="Alınan Miktar" FontSize="12" Foreground="#34495E" FontWeight="SemiBold"/>
                        <TextBox Padding="10" FontSize="14" Text="{Binding AmountText}"/>
                    </StackPanel>

                    <StackPanel Grid.Column="2" Spacing="5">
                        <TextBlock Text="Birim Fiyatı (₺)" FontSize="12" Foreground="#34495E" FontWeight="SemiBold"/>
                        <TextBox Padding="10" FontSize="14" Text="{Binding UnitPriceText}"/>
                    </StackPanel>
                </Grid>

                <TextBlock Text="Aracı Kurum Masrafı / Komisyon (₺)" FontSize="12" Foreground="#34495E" FontWeight="SemiBold"/>
                <TextBox Padding="10" FontSize="14" Margin="0,0,0,10" Text="{Binding FeeText}"/>

                <TextBlock Text="Ödeme Kaynağı (İsteğe Bağlı)" FontSize="12" Foreground="#34495E" FontWeight="SemiBold"/>
                <TextBlock Text="Seçilen hesabın bakiyesi otomatik olarak düşürülecektir." FontSize="11" Foreground="#7F8C8D" TextWrapping="Wrap"/>
                <ComboBox Padding="10" FontSize="14" Margin="0,0,0,10" HorizontalAlignment="Stretch" ItemsSource="{Binding Accounts}" SelectedItem="{Binding SelectedAccount}"/>
            </StackPanel>
        </ScrollViewer>

        <!-- Toplam Maliyet Önizlemesi -->
        <Border Grid.Row="2" Background="#EBF5FB" CornerRadius="8" Padding="15" Margin="0,5,0,0" BorderBrush="#AED6F1" BorderThickness="1">
            <Grid ColumnDefinitions="*,Auto">
                <TextBlock Grid.Column="0" Text="Toplam Maliyet:" FontSize="14" FontWeight="SemiBold" Foreground="#2C3E50" VerticalAlignment="Center"/>
                <TextBlock Grid.Column="1" Text="{Binding TotalCostPreviewText}" FontSize="18" FontWeight="Bold" Foreground="#2980B9" VerticalAlignment="Center"/>
            </Grid>
        </Border>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Content="İptal" Width="90" Padding="10" Command="{Binding CancelCommand}" Background="#ECF0F1" BorderThickness="0" Margin="0,0,10,0" HorizontalContentAlignment="Center"/>
            <Button Content="Satın Al" Width="120" Padding="10" Command="{Binding SaveCommand}" Background="#27AE60" Foreground="White" FontWeight="Bold" BorderThickness="0" HorizontalContentAlignment="Center"/>
        </StackPanel>
    </Grid>
</Window>
